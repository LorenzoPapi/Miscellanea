# coding: utf-8
from requests import Session
s = Ses
URL = 'https://scrollspeed.com/'
def new_record(name):
    s.post(URL + 'ajax/save.php', data={'score': 2**64-1, 'distscroll': -2**100})
    s.post(URL + 'ajax/addName.php', data={'dataValue': name})
    
new_record('hello')
def new_record(name):
    s = Session()
    s.get(URL)
    s.post(URL + 'ajax/save.php', data={'score': 2**64-1, 'distscroll': -2**100})
    s.post(URL + 'ajax/addName.php', data={'dataValue': name})
    
new_record('hello')
new_record('HelloMyNameIsMarkiplier')
hello = "Mark: Hello everybody, my name is Markiplier and welcome to Five Nights at Freddy's, an indie horror game that you guys suggested, en masse, and I saw that Yamimash played it and he said it was really really good... So I'm very eager to see what is up. And that is a terrifying animatronic bear! 'Family pizzeria looking for security guard to work the nightshift.' Oh... 12 a.m. The first night. If I didn't wanna stay the first night, why would I stay any more than... five... Why I stay any more than two- hello? Okay..."
hello
new_record(hello)
def new_record(name):
    s = Session()
    s.get(URL)
    print(s.post(URL + 'ajax/save.php', data={'score': 2**64-1, 'distscroll': -2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record(hello)
def new_record(name):
    s = Session()
    s.get(URL)
    s.post(URL + 'ajax/save.php', data={'score': 1.056.473.829.362.304, 'distscroll': -2**100})
    s.post(URL + 'ajax/addName.php', data={'dataValue': name})
def new_record(name):
    s = Session()
    s.get(URL)
    s.post(URL + 'ajax/save.php', data={'score': 1056473829362304, 'distscroll': -2**100})
    s.post(URL + 'ajax/addName.php', data={'dataValue': name})
    
new_record('Hello everybody my name is Markiplier')
def new_record(name):
    s = Session()
    s.get(URL)
    print(s.post(URL + 'ajax/save.php', data={'score': 1056473829362304, 'distscroll': -2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record('Hello everybody my name is Markiplier')
new_record('Helloeverybodymynameismarkiplier')
def new_record(name):
    s = Session()
    s.get(URL)
    print(s.post(URL + 'ajax/save.php', data={'score': 2**32, 'distscroll': -2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record('Helloeverybodymynameismarkiplier')
def new_record(score, name):
    s = Session()
    s.get(URL)
    print(s.post(URL + 'ajax/save.php', data={'score': score, 'distscroll': -2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record(2**48,"testing")
2**64
2**63
new_record(2**56,"Spyro102")
new_record(2**60,"StarFruit1234")
new_record(2**62,"StarFruit1234")
2**52
2**62
new_record(2**62,"syspfp")
new_record(2**63,"imcominghome")
new_record(-1,"amITheLastOne")
new_record(2**62*3//2,"almosthere")
new_record(56000,"SameAsYourName")
new_record(56000,"ASameAsYourName")
new_record(2**1024,"ABitLargeInnit")
new_record(10**4035,"ATinyBitLargeInnit")
new_record("1e10000","ATinyBitLargeInnit")
new_record("1e1","ATinyBitLargeInnit")
new_record(-10**4036,"Too much negativity")
new_record(1.10,"Too much negativity")
new_record(1.10,"Fractions too?")
minv, maxv = 2**62, 2**63
from bs4 import BeautifulSoup
while minv != maxv:soup = BeautifulSoup(get('https://scrollspeed.com/WorldRanking').content, 'html.parser')
while minv != maxv:soup = BeautifulSoup(get('https://scrollspeed.com/WorldRanking').content, 'html.parser')
while minv != maxv: soup = BeautifulSoup(get('https://scrollspeed.com/WorldRanking').content, 'html.parser')
while minv != maxv:
    user = random.choice(usernames)
    mid = (maxv + minv) // 2
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    
import random
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr'))
    
import string
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr'))
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    mid = -mid
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr'))
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr').find_all('td'))
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr').find_all('td')[1])
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    print(soup.find('tr').find_all('td')[1].text)
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    print(is_first)
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    user = 'almosthere'
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    print(is_first)
    
if minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    #user = 'almosthere'
    mid = (maxv + minv) // 2
    mid = -mid
    #new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    if is_first:
        minv = mid +1
    else:
        maxv = mid
    print(is_first)
    
minv = (maxv + minv)) // 2
minv = (maxv + minv) // 2
while minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    #user = 'almosthere'
    mid = (maxv + minv) // 2
    mid = -mid
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    if is_first:
        minv = mid +1
    else:
        maxv = mid
    print(is_first)
    
minv
maxv
while minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    #user = 'almosthere'
    mid = (maxv + minv) // 2
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    if is_first:
        minv = mid +1
    else:
        maxv = mid
    print(is_first)
    
minv
maxv
minv = 2**62*3//2
minv
maxv = 2**63
while minv != maxv:
    user = ''.join(random.choices(string.ascii_letters + string.digits, k = random.choice(range(6, 18))))
    #user = 'almosthere'
    mid = (maxv + minv) // 2
    new_record(mid, user)
    s = Session()
    soup = BeautifulSoup(s.get(URL + 'WorldRanking').content, 'html.parser')
    actual_first = soup.find('tr').find_all('td')[1].text
    is_first = actual_first == user
    if is_first:
        minv = mid +1
    else:
        maxv = mid
    print(is_first)
    
new_record(2**63 -1, "pretty big")
new_record(2**63, "pretty big")
new_record(2**63, "pretty biggy")
def new_record(score, name):
    s = Session()
    s.get(URL)
    print(s.post(URL + 'ajax/save.php', data={'score': score, 'distscroll': 2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record(2**63-1, "canyouplease")
s = Session()
s.get(URL)
def new_record(score, name):
    global s
    print(s.post(URL + 'ajax/save.php', data={'score': score, 'distscroll': 2**100}).content)
    print(s.post(URL + 'ajax/addName.php', data={'dataValue': name}).content)
    
new_record(10, 'QuirkYgirl10')
new_record(100, 'QuirkYgirl1')
new_record(100, 'Quir')
new_record(2**63 - 1, 'Help')
new_record(2**63 - 1, 'LorenzoFottutoMontano')
new_record(2**63 - 1, 'TUTTOQUESTOMIFAVOLARE')
new_record(2**63 - 1, 'LorenzoFottutoMontano')
new_record(2**63 - 1, 'BuonaseraGruppoCiaoGiovanni')
new_record(2**63 - 1, 'BuonaseraGruppoCiaoGiovanni')
lorenzo_session = s
s
lorenzo_session
lorenzo_session = s.copy()
s = None
lorenzo_session 
s
s = Session()
s.get(URL)
new_record(-10**8000, 'NEGATIVE')
import sys
sys.set_int_max_str_digits(
)
sys.set_int_max_str_digits(2**16)
new_record(-10**8000, 'NEGATIVE')
new_record(10**4000, 'POSITIVE')
new_record(10**200, 'HIGHER')
new_record(10**70, 'HIGHERS')
s.clear()
s.cookies.clear()
s.get(URL)
new_record(10**50, 'HIGHERS')
new_record(10**50, 'HIGHER')
new_record(10**70, 'HIGHERL')
new_record(10**100, 'HIGHER')
